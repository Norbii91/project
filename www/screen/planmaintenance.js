/**
 * Tervek kirajzolása
 */

async function redrawPlans() {
    const table = document.getElementById('plantable')

    for (const s of window.plans) {
        const tr = document.createElement('tr')
    }
}
